---
interface Props {
  imageDesktop?: string;
  imageMobile?: string;
  title: string;
}

const { imageDesktop, imageMobile, title } = Astro.props;

function formatTitle(title: string) {
  const words = title.split(" ");
  return words.length === 2 ? `${words[0]}<br>${words[1]}` : title;
}
---

<article
  class="group relative h-29 bg-cover bg-center bg-no-repeat hover:cursor-pointer sm:h-64 xl:h-112"
  style={`--bg-mobile: url(${imageMobile}); --bg-desktop: url(${imageDesktop});`}
>
  <a href={import.meta.env.BASE_URL} aria-label={title}>
    <div
      class="transition-background absolute inset-0 z-0 bg-linear-to-b from-black/0 to-black/60 duration-300 hover:bg-theme-white/70"
      aria-hidden="true"
    >
    </div>
    <p
      class="force-two-lines pointer-events-none absolute bottom-5 z-1 w-48 pl-5 font-josefinsans text-2xl leading-6 text-theme-white uppercase transition-colors duration-300 group-hover:font-bold group-hover:text-theme-black md:bottom-8 md:w-52 md:pl-10 md:text-3xl md:leading-none"
      set:html={formatTitle(title)}
      aria-hidden="true"
    />
  </a>
</article>

<style>
  article {
    background-image: var(--bg-mobile);
  }
  @media (min-width: 640px) {
    article {
      background-image: var(--bg-desktop);
    }
  }
</style>
